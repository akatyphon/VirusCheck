<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VirusCheck" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Inline ColorStateList for BottomNavigationView -->
    </style>

    <style name="Theme.VirusCheck" parent="Base.Theme.VirusCheck"/>

    <style name="Theme.VirusCheck.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#43ADFF</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/app_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.VirusCheck</item>
    </style>

</resources>